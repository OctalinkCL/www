---
import { MapPin, ArrowRight, Play } from "@lucide/astro";
---

<section class="relative min-h-screen flex items-center overflow-hidden">
  <!-- Background Effects -->
  <div class="absolute inset-0 bg-gradient-radial"></div>
  <div class="absolute inset-0 bg-grid opacity-50"></div>
  
  <!-- Floating Orbs -->
  <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-orange-500/10 rounded-full blur-[120px] animate-float"></div>
  <div class="absolute bottom-1/4 right-1/4 w-64 h-64 bg-orange-600/5 rounded-full blur-[100px] animate-float" style="animation-delay: -3s;"></div>

  <!-- Content -->
  <div class="wrapper relative z-10 pt-32 pb-20 md:pt-40 md:pb-32">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 mb-8 animate-fade-in-up">
        <span class="section-badge">
          <MapPin size={16} />
          Based in South America, Chile
        </span>
      </div>

      <!-- Title -->
      <h1 class="section-title mb-6 animate-fade-in-up" style="animation-delay: 0.1s;">
        Tu empresa es sólida.
        <br />
        <span class="gradient-text">Tu presencia digital también debería serlo.</span>
      </h1>

      <!-- Subtitle -->
      <p class="section-subtitle max-w-2xl mx-auto mb-10 animate-fade-in-up" style="animation-delay: 0.2s;">
        No vendemos páginas web, somos tu equipo de infraestructura digital. 
        Nos encargamos de que todo funcione siempre — suscripción mensual fija sin costos ocultos.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center animate-fade-in-up" style="animation-delay: 0.3s;">
        <a href="#modelo" class="btn-primary">
          <span>Ver Modelo WaaS</span>
          <ArrowRight size={20} />
        </a>
        <a href="#servicios" class="btn-secondary">
          <Play size={18} />
          <span>Explorar Servicios</span>
        </a>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-3 gap-8 mt-20 pt-10 border-t border-white/10 animate-fade-in-up" style="animation-delay: 0.4s;">
        <div class="text-center">
          <div class="text-3xl md:text-4xl font-bold text-white mb-2">99.9%</div>
          <div class="text-sm text-neutral-500">Uptime garantizado</div>
        </div>
        <div class="text-center">
          <div class="text-3xl md:text-4xl font-bold text-white mb-2">24/7</div>
          <div class="text-sm text-neutral-500">Monitoreo activo</div>
        </div>
        <div class="text-center">
          <div class="text-3xl md:text-4xl font-bold text-white mb-2">$0</div>
          <div class="text-sm text-neutral-500">Inversión inicial</div>
        </div>
      </div>
    </div>

    <!-- Hero Visual -->
    <div class="mt-16 md:mt-24 relative animate-fade-in-up" style="animation-delay: 0.5s;">
      <div class="relative rounded-3xl overflow-hidden glow-orange">
        <!-- Gradient Border Effect -->
        <div class="absolute inset-0 rounded-3xl p-[1px] bg-gradient-to-b from-white/20 to-transparent pointer-events-none"></div>
        
        <!-- Video Container -->
        <div class="relative aspect-video rounded-3xl overflow-hidden">
          <video
            src="https://framerusercontent.com/assets/iCQ1bxRMFTWrEalWCWNNBrJPuWk.mp4"
            autoplay
            muted
            loop
            playsinline
            class="w-full h-full object-cover"
          ></video>
          
          <!-- Video Overlay -->
          <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"></div>
          
          <!-- Bottom Info Bar -->
          <div class="absolute bottom-0 left-0 right-0 p-6 md:p-8">
            <div class="glass rounded-2xl p-4 md:p-6 flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
              <div class="flex items-center gap-4">
                <div class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></div>
                <span class="text-sm text-neutral-300">Sistema operativo al 100%</span>
              </div>
              <div class="flex items-center gap-6 text-sm">
                <div class="flex items-center gap-2">
                  <span class="text-neutral-500">Latencia:</span>
                  <span class="text-green-400 font-medium">12ms</span>
                </div>
                <div class="flex items-center gap-2">
                  <span class="text-neutral-500">SSL:</span>
                  <span class="text-green-400 font-medium">Activo</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
    <div class="w-6 h-10 rounded-full border-2 border-white/20 flex items-start justify-center p-2">
      <div class="w-1 h-2 bg-white/50 rounded-full"></div>
    </div>
  </div>
</section>
